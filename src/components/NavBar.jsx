import React from 'react';
import "./home.css"

export const NavBar = () => {
    return (
        <div>
            <nav>
                <a href="#">Home</a>
                <a href="#">Services</a>
                <a href="#">About Us</a>
                <a href="#">Contact Us</a>
            </nav>
        </div>
    );
};

